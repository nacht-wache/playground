name: Ubuntu Clang Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  call-workflow:
    uses: ./.github/workflows/ubuntu_base.yml
    with:
      c_compiler: clang
      cpp_compiler: clang++